[workspace]
members = [
    "cdk-indexer.v2",
    "merkle-trie-generator",
]
exclude = [
    "cdk-indexer.v1.legacy",
]

# edition = "2021"
resolver = "2"
overflow-checks = true

[profile.release]
strip = true
lto = true
panic = "abort"
codegen-units = 1
