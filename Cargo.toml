[workspace]
members = [
    "cdk-indexer.v3",
]
exclude = [
    "cdk-indexer.v1.ethers",
    "cdk-indexer.v2.alloy",
    "merkle-trie-generator",
]

# edition = "2021"
resolver = "2"
overflow-checks = true

[profile.release]
strip = true
lto = true
panic = "abort"
codegen-units = 1

