[workspace]
members = [
    "events-monitor",
    "abi-fetcher",
]
exclude = [
    "legacy",
    "cdk-indexer.v1.ethers",
    "cdk-indexer.v2.alloy",
    "cdk-indexer.v3.prototype",
    "cdk-indexer.v4.prototype",
    "merkle-trie-generator",
]

# edition = "2021"
resolver = "2"
overflow-checks = true

[profile.release]
strip = true
lto = true
panic = "abort"
codegen-units = 1

